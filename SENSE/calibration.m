function camStats = calibration(camera, target)
    %%% Calibrates the camera automatically
    % INPUTS:
        % camera: camera object to calibrate
        % target: image of fiducial marker adjusted to what camera should
        %         see it as
    % OUTPUTS: dict of camera's calibrated statistics (can be removed)
    
    
    

end
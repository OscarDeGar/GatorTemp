function camStats = calibration(camera, target)
%%% Calibrates the camera automatically
    % INPUTS: camera(object) - camera object for calibration
    %         target(imgTYPE??) - image of fiducial marker adjusted to what 
    %                             camera should see it as
    % OUTPUTS: camStats(struct) - camera's calibrated statistics (can be removed)
    
    
    

end